   
              Code pen link:
   
   https://codepen.io/tasnimzalom/pen/bGeROdm?editors=1100

   
                Questions:   
   
    1. What is Semantic HTML? 
HTML with specific meaning.
    2. What is HTML used for? 
Structure. It allows us, as develepors to create and apply headings, links, sections, paragraphs, and set up the formatting.
    3. What is an attribute and where do we put it? 
An attribute is a way to link sites, or images, that would redirect the user out of the current webpage. It should be inside the  
    4. What is the h1 tag used for? How many times should I use it on a page?
h1 is the most important title on the page, and you should only use it once per page.
    5. Name two tags that have required attributes
The <img> tag has two required elements alt and src. and  
    6. What do we put in the head of our HTML document? 
<!DOCTYPE html> <html>
    7. What is an id? 
An attribute that specifies and element.
    8. What elements can I add an id to?
Any element.
    9. How many times can I use the same id on a page?
 Ids can only be used once
    10. What is a class? 
An attribute that specifies html elements.
    11. What elements can I add a class to?
 Any of them.
    12. How many times can I use the same class on a page? 
As many as you like.
    13. How do I get my link to open in a new tab?
<a href="Your Link" target="_blank"> 
    14. What is the alt attribute in the image tag used for? 
For accessibility( it helps screen reader's describe an image to the visually-impaired.)
    15. How do I reference an id?
"#"{
property:value;
}
    16. What is the difference between a section and a div
<section> is semantic whereas <div> is just regular html
    17. What is CSS used for? 
styling the webpage.
    18. How to we select an element? Example - every h2 on the page
h2{
  property:value;
}
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
Classes can be used multiple times in one page whereas Id's can only be used to target one thing.

<div class="colors">blue</div>
<div class="colors">red</div>

<h1 id="section-1">Hello world</h1>

    20. How do we select classes in CSS?
.class{
property:value;
}
    21. How do we select a p element with a single class of “human””?
p.human{
property:value;
}
    22. What is a parent child selector? When would this be useful? 
A parent child selector selects all the elements that are direct children of the specified element. 
parent>child{
  propert:value;
}
    23. How do you select all links within a div with the class of sidebar?
.sidebar a{
propert:value;
}
    24. What is a pseudo selector?
A selector thats used to style the elements that are either the first of their type of hovered over.
    25. What do we use the change the spacing between lines?
line-height: value; line-width: value;
    26. What do we use to change the spacing between letters?
letter-spacing: value;
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
Text-transformation: value;
    28. How do I add a 1px border around my div that is dotted and black?
div{
border: 1px dotted black;
}
    29. How do I select everything on the page? 
using the asterisk "*" so:
*{
  property: value;
}
    30. How do I write a comment in CSS?
 "ctrl /" or /* comment */
    31. How do I find out what file I am in, when I am using the command line? 
ls
    32. Using the command line - how do I see a list of files/folders in my current folder?
The terminal will show you the path each line you write.
    33. How do I remove a file via the command line? Why do I have to be careful with this? 
rm "filename" It will permanently delete without a confirmation option or warning.
    34. Why should I use version control? 
To give viewing access to others and for collaboration.
    35. How often should I commit to github?
Every 20 minutes or so.
    36. What is the command we would use to push our repo up to github? 
git push -u origin firstname-lastname
    37. Walk me through Lambda's git flow. 

Stretch Questions

    1. What is the difference between an inline element and a block element? 
    -Inline elements can sit next to other elements and doesnt add a line break, whereas the block element starts a new line and stretches out as far as it can in either direction(left or right).

    2. What happens when an element is positioned absolutely? 
    -when an element is positioned absolutely, its position changes according to the nearest "fmaily-member" element. If there are no parent or grandparents nearby it will adjust to the page.

    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? 
    -

    4. Name 3 elements that are display block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
    -display block: <li> <nav> <p>  -display inline: <a> <span> <i>      -display inline-block:<img>   

    5. In your own words, explain the box model. What is the fix for the box model? 
    -Everything is a box. The core is the content, the padding is the space directly around the content, the margin is the space around the padding, (the width of the perimeter) and the border is the the outer-most part of the box.

